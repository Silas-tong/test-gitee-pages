<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
    <title>Gitee Pages 测试站 | 静态托管演示</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="container">
        <div class="card">
            <h1>🚀 部署成功！</h1>
            <p>如果你能看到这个页面，说明 <strong>Gitee Pages 静态托管</strong> 已经正常工作。</p>
            <div class="badge">
                <span>✅ 托管状态：在线</span>
                <span>📍 托管平台：Gitee Pages</span>
            </div>
        </div>

        <div class="card">
            <h2>📊 访客计数器（演示JS功能）</h2>
            <p>点击下方按钮，数字会保存到浏览器的本地存储中，刷新页面也不会丢失。</p>
            <div class="counter-area">
                <div class="counter-display">
                    您访问本站的次数: <span id="visitCount">0</span> 次
                </div>
                <div class="counter-buttons">
                    <button id="incrementBtn">👍 点我 +1</button>
                    <button id="resetBtn">🔄 重置计数</button>
                </div>
            </div>
            <p class="note">※ 计数器数据仅保存在您自己的浏览器中（localStorage），不同访客互不影响。</p>
        </div>

        <div class="card">
            <h2>🧪 下一步测试建议</h2>
            <ol>
                <li>修改这个 <code>index.html</code> 里的文字或样式</li>
                <li>重新上传到 Gitee 仓库</li>
                <li>等待 1-2 分钟后刷新 Gitee Pages 网址，看是否自动更新</li>
                <li>如果需要“多人共享数据”，可后续接入 LeanCloud / Bmob 等后端云</li>
            </ol>
            <div class="footer-note">
                当前时间: <span id="currentTime"></span>
            </div>
        </div>
    </div>
    <script>
        // 显示当前时间（辅助验证JS是否执行）
        const timeSpan = document.getElementById('currentTime');
        if(timeSpan) {
            timeSpan.innerText = new Date().toLocaleString('zh-CN');
            setInterval(() => {
                timeSpan.innerText = new Date().toLocaleString('zh-CN');
            }, 1000);
        }

        // 访客计数器 (localStorage)
        const STORAGE_KEY = "gitee_pages_test_visits";
        let visitCountSpan = document.getElementById('visitCount');
        let incrementBtn = document.getElementById('incrementBtn');
        let resetBtn = document.getElementById('resetBtn');

        // 读取已保存的次数
        let currentCount = localStorage.getItem(STORAGE_KEY);
        if(currentCount === null) {
            currentCount = 0;
        } else {
            currentCount = parseInt(currentCount, 10);
        }
        visitCountSpan.innerText = currentCount;

        // +1 按钮
        if(incrementBtn) {
            incrementBtn.addEventListener('click', function() {
                currentCount++;
                localStorage.setItem(STORAGE_KEY, currentCount);
                visitCountSpan.innerText = currentCount;
            });
        }

        // 重置按钮
        if(resetBtn) {
            resetBtn.addEventListener('click', function() {
                currentCount = 0;
                localStorage.setItem(STORAGE_KEY, currentCount);
                visitCountSpan.innerText = currentCount;
            });
        }
    </script>
</body>
</html>
